zig-out
zig-cache
.zig-cache
main
main.o
*gb
.DS_Store
# WebAssembly build artifacts / scratch outputs
*.wasm
*.wasm.o
*.o
# If you want to keep local test ROMs out of commits remove the exception; games already ignored.
.vscode
dump*
EpicLog*
notes*
Blarg*
games
DUMP*
emutrace*

# Transient golden-harness output (compared against tools/golden/*.txt)
tools/golden/*.cur.txt

# Keep source files (e.g., web.zig) committed; patterns above only affect compiled outputs.
